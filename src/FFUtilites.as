/**
 * @author vasily.timofeev@gmail.com
 */
package
{
    import com.timoff.ql.Query;
    import com.timoff.services.cache.Cache;
    import com.timoff.services.loader.managers.BasicLoadManager;
    import com.timoff.services.log.Log;
    import com.timoff.services.so.SharedObjectManager;
    import com.timoff.services.test.BasicTest;
    import com.timoff.services.time.BasicTimerObject;
    import com.timoff.utilites.*;
    import com.timoff.utilites.draw.DashedSprite;
    import ru.inspirit.net.MultipartURLLoader;

    public class FFUtilites
    {
        Query;
        BasicLoadManager;
        Log;
        SharedObjectManager;
        Cache;
        BasicTest;
        BasicTimerObject;

        DashedSprite;
        DisplayObjectUtils;
        ImageUtils;
        Library;
        NameUtils;
        ObjectUtils;
        TimeUtils;

        MultipartURLLoader;
    }
}
